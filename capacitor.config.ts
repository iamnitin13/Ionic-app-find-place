import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.ap9city',
  appName: 'ap9city',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
